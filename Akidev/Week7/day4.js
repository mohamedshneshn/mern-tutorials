/*

Database Class:
-----------------
Create a class that is used to manage a database of users. The class should have
the following methods:



 public static void DBClassMethod(){
    
    list<account> acc = new list<account>();
    
    for(integer i =0 ; i<5 ; i++){
        
        if(i==3){
            acc.add(new account());
        }else {
        acc.add(new account(name='DBClass'+i));
        }
        
    }
        database.saveresult[] results = database.insert(acc,false);
        system.debug(acc);
        system.debug(results );
    }









*/
